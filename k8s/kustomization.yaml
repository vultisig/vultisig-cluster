apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Default: Use local overlay (deploys everything locally)
# For production endpoints: kubectl apply -k k8s/overlays/production

resources:
  - overlays/local
